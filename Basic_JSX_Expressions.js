// we first import our libraries
// ReactDOM.render() is the main function to output text to the screen
import React from 'react';
import ReactDOM from 'react-dom';

// here we send 2 arguments to the ReactDOM.render functions
ReactDOM.render(<h1>Render me!</h1>,
document.getElementById('app'))
